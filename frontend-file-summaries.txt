{
  "frontend/src/index.css": "Global styles with OKLCH deep blue + orange color system, Inter/Poppins font imports, hero overlay utility, and rail/orange gradient classes for the RailMutual Connect theme.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Inter/Poppins font families, deep blue primary palette, orange accent colors, custom card shadows, and fade-in animation.",
  "frontend/src/App.tsx": "Main app with TanStack Router — public routes (landing, auth) and protected routes (dashboard, profile, matches) wrapped in `ProtectedLayout`; unauthenticated users are redirected to `/auth` via a `RedirectToAuth` component instead of rendering `AuthPage` with props.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations; the `useUpdateProfile` fallback path now explicitly maps each field onto the existing profile and converts `null` email/phone to `undefined` to satisfy the `Profile` type's optional string fields.",
  "frontend/src/components/Layout.tsx": "Authenticated layout wrapper with sticky deep-blue navigation bar (logo, nav links, logout), mobile hamburger menu, and branded footer with disclaimer and caffeine.ai attribution.",
  "frontend/src/pages/LandingPage.tsx": "Public landing page with hero section (banner image, headline, tagline), features grid, how-it-works steps, stats bar, important notice, and footer — all in deep blue + orange theme.",
  "frontend/src/pages/AuthPage.tsx": "Authentication page using Internet Identity; after login uses the cached `registerOrLogin` result from actor initialization to determine whether to redirect to profile setup or dashboard, with proper error handling and loading states.",
  "frontend/src/pages/ProfilePage.tsx": "Profile setup/edit form with all required fields pre-filled from backend data; on submit calls `updateProfile` mutation, shows success feedback, and navigates to dashboard after a short delay.",
  "frontend/src/pages/DashboardPage.tsx": "Protected dashboard showing greeting, profile completion progress, mutual match count, quick action buttons, profile summary grid, and posting swap banner — with skeleton loading states and graceful error handling.",
  "frontend/src/components/MatchCard.tsx": "Reusable match card displaying employee profile details (name, designation, zone, division, posting swap), with a Connect button that reveals contact info or shows privacy message based on contactVisible.",
  "frontend/src/pages/MatchesPage.tsx": "Protected matches page with mutual matches grid, search/filter panel (zone dropdown, division/location/designation inputs), tab switcher between mutual matches and search results, and empty state handling.",
  "frontend/src/hooks/useActor.ts": "Actor initialization hook that creates the backend actor with the user's identity, calls `_initializeAccessControlWithSecret` to set up roles, and for authenticated users also calls `registerOrLogin()` to ensure the user record and `#user` role exist before any other queries run."
}